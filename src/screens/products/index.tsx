import React, { FC } from 'react';
import { PageWrapper } from '../../components/globals/styles';

const Products: FC = () => {
  return <PageWrapper>Choose Product Screen</PageWrapper>;
};

export default Products;
