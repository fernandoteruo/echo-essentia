import React, { FC } from 'react';
import { PageWrapper } from '../../components/globals/styles';

const Volume: FC = () => {
  return <PageWrapper>Choose Volume Screen</PageWrapper>;
};

export default Volume;
