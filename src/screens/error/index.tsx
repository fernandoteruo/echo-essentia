import React, { FC } from 'react';
import { PageWrapper } from '../../components/globals/styles';

const Error: FC = () => {
  return <PageWrapper>Error Screen</PageWrapper>;
};

export default Error;
